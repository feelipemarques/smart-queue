import { WaitTimePipe } from './wait-time-pipe';

describe('WaitTimePipe', () => {
  it('create an instance', () => {
    const pipe = new WaitTimePipe();
    expect(pipe).toBeTruthy();
  });
});
